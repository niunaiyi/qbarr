# 应用端口
PORT=8011

# Node 环境
NODE_ENV=production

# 要监控的容器名称（逗号分隔，不区分大小写）
# 支持部分匹配，例如 "arr" 会匹配所有 *arr 系列容器
# 默认值 "arr" 会监控: radarr, sonarr, lidarr, readarr, prowlarr, whisparr 等
MONITORED_CONTAINERS=Radarr, Sonarr, Lidarr, Prowlarr, Whisparr

# 其他示例配置：
# MONITORED_CONTAINERS=nginx,mysql,redis
# MONITORED_CONTAINERS=web,api,worker

# ==========================================
# 文件夹监控配置（自动启停容器）
# ==========================================

# 是否启用文件夹监控
MONITOR_ENABLED=true

# 监控的路径（容器内的路径）
MONITOR_PATH=/downloads

# 排除的文件夹名称（不计入大小）
EXCLUDE_FOLDER=incomplete

# 启动容器的阈值（GB）- 当文件夹大小 >= 此值时，启动所有 *arr 容器
START_THRESHOLD_GB=100

# 停止容器的阈值（GB）- 当文件夹大小 <= 此值时，停止所有 *arr 容器
STOP_THRESHOLD_GB=50

